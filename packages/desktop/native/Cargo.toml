[package]
name = "watchdesk-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["async", "napi9"] }
napi-derive = "2"
portable-pty = "0.8"
futures = "0.3"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
